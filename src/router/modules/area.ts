import { IRouter } from 'router';
import { MapInformation2Icon } from 'tdesign-icons-react';

const area: IRouter[] = [
  {
    path: '/area',
    meta: {
      title: '地区',
      Icon: MapInformation2Icon,
    },
  },
];

export default area;
