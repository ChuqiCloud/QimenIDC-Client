import { IRouter } from 'router';
import { AirplayWaveIcon } from 'tdesign-icons-react';

const vm: IRouter[] = [
  {
    path: '/vm',
    meta: {
      title: '虚拟机',
      Icon: AirplayWaveIcon,
    },
  },
];

export default vm;
