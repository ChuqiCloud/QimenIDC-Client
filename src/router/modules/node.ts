import { IRouter } from 'router';
import { CpuIcon } from 'tdesign-icons-react';

const node: IRouter[] = [
  {
    path: '/node',
    meta: {
      title: '节点',
      Icon: CpuIcon,
    },
  },
];

export default node;
